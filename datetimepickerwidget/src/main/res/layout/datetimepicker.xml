<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
	android:layout_width="fill_parent" android:layout_height="wrap_content" android:fitsSystemWindows="true" 
	android:padding="5dip" android:id="@+id/DateTimePicker">
	<LinearLayout android:id="@+id/ViewSwitchButtons" android:layout_width="fill_parent"
		android:layout_height="wrap_content" android:layout_marginBottom="5dip" >

		<Button
		    android:id="@+id/SwitchToDate"
		    android:layout_width="0dip"
		    android:layout_height="wrap_content"
		    android:layout_weight="1"
		    android:drawableLeft="@drawable/ic_menu_month"
		    android:drawableStart="@drawable/ic_menu_month"
		    android:enabled="false"
		    android:text="@string/btn_setdate" />

		<Button
		    android:id="@+id/SwitchToTime"
		    android:layout_width="0dip"
		    android:layout_height="wrap_content"
		    android:layout_weight="1"
		    android:drawableLeft="@drawable/ic_action_time"
		    android:drawableStart="@drawable/ic_action_time"
		    android:text="@string/btn_settime" />

	</LinearLayout>
	<ViewSwitcher android:layout_width="fill_parent"  android:layout_below="@+id/ViewSwitchButtons"
		android:layout_height="wrap_content" android:id="@+id/DateTimePickerVS" />
</RelativeLayout>